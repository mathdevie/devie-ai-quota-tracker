<svg width="120" height="120" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="blur-copper-sunset" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="18"/>
    </filter>
    <filter id="grain-copper-sunset" x="0" y="0" width="100%" height="100%">
      <feTurbulence type="fractalNoise" baseFrequency="0.7" numOctaves="4" seed="42" result="noise"/>
      <feColorMatrix type="saturate" values="0" in="noise" result="mono"/>
      <feBlend in="SourceGraphic" in2="mono" mode="overlay"/>
    </filter>
    <clipPath id="clip-copper-sunset">
      <rect width="120" height="120" rx="16" ry="16"/>
    </clipPath>
  </defs>
  
  <g clip-path="url(#clip-copper-sunset)">
    <!-- Solid background base -->
    <rect width="120" height="120" fill="#fff6eb"/>
    
    <!-- Fixed position blobs - diagonal gradient from top-left to bottom-right -->
    <!-- Main primary blob - bottom right -->
    <ellipse cx="100" cy="100" rx="70" ry="65" fill="#d87833" filter="url(#blur-copper-sunset)"/>
    
    <!-- Secondary blob - middle area -->
    <ellipse cx="65" cy="75" rx="45" ry="40" fill="#d87833" filter="url(#blur-copper-sunset)"/>
    
    <!-- Light accent - transition zone -->
    <ellipse cx="45" cy="55" rx="35" ry="30" fill="#e6a77a" filter="url(#blur-copper-sunset)"/>
    
    <!-- Dark accent - corner depth -->
    <ellipse cx="115" cy="115" rx="40" ry="35" fill="#a25a26" filter="url(#blur-copper-sunset)"/>
    
    <!-- Subtle grain overlay -->
    <rect width="120" height="120" fill="#fff6eb" filter="url(#grain-copper-sunset)" opacity="0.1"/>
  </g>
  
  <!-- Rounded border -->
  <rect width="120" height="120" rx="16" ry="16" fill="none" stroke="#d87833" stroke-width="0.5" stroke-opacity="0.15"/>
</svg>