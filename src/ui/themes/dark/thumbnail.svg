<svg width="120" height="120" viewBox="0 0 120 120" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="blur-dark" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur stdDeviation="18"/>
    </filter>
    <filter id="grain-dark" x="0" y="0" width="100%" height="100%">
      <feTurbulence type="fractalNoise" baseFrequency="0.7" numOctaves="4" seed="42" result="noise"/>
      <feColorMatrix type="saturate" values="0" in="noise" result="mono"/>
      <feBlend in="SourceGraphic" in2="mono" mode="overlay"/>
    </filter>
    <clipPath id="clip-dark">
      <rect width="120" height="120" rx="16" ry="16"/>
    </clipPath>
  </defs>
  
  <g clip-path="url(#clip-dark)">
    <!-- Solid background base -->
    <rect width="120" height="120" fill="#131313"/>
    
    <!-- Fixed position blobs - diagonal gradient from top-left to bottom-right -->
    <!-- Main primary blob - bottom right -->
    <ellipse cx="100" cy="100" rx="70" ry="65" fill="#d1d1d1" filter="url(#blur-dark)"/>
    
    <!-- Secondary blob - middle area -->
    <ellipse cx="65" cy="75" rx="45" ry="40" fill="#d1d1d1" filter="url(#blur-dark)"/>
    
    <!-- Light accent - transition zone -->
    <ellipse cx="45" cy="55" rx="35" ry="30" fill="#e1e1e1" filter="url(#blur-dark)"/>
    
    <!-- Dark accent - corner depth -->
    <ellipse cx="115" cy="115" rx="40" ry="35" fill="#9d9d9d" filter="url(#blur-dark)"/>
    
    <!-- Subtle grain overlay -->
    <rect width="120" height="120" fill="#131313" filter="url(#grain-dark)" opacity="0.1"/>
  </g>
  
  <!-- Rounded border -->
  <rect width="120" height="120" rx="16" ry="16" fill="none" stroke="#d1d1d1" stroke-width="0.5" stroke-opacity="0.15"/>
</svg>